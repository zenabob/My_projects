Zenab Mahmoud 

description:
A server-side application that manages the registration of students for courses with (node js with express) Backend language  and (MongoDB with mogoose) Database because it NoSQl data base (that dont use the traditional table-based structure of relational databases ) and it easiest way to mdel data and it validate documants before they are inserted into a collection and provide advanced custom data types or plugings. 

what need to install to run it?
1)Download node js by : https://nodejs.org/en/download
2)npm init -y
3)open the terminal and put : node install express body-parser express-validator


How to run it ?
1) open the terminal.
2) put "node server.js"
3) put in the browser "http://localhost:8080"

